node_modules
dist
coverage
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Secrets — .env is encrypted via dotenvx and safe to commit
.env.*
!.env
!.env.example

# Local-only agent settings
.claude/settings.local.json
.claude/worktrees/
CLAUDE.local.md
.codex/private/
.codex/environments/*.local.*
docs/evals/private/

# History-rewrite tooling artifacts (must never be tracked)
.git-rewrite/
